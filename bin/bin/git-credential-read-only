#!/bin/bash
protocol="https"
host="github.com"
pw=$(pass Productivity/github.com)
password=$(sed -n '1p' <<< $pw)
username=$(echo $pw | grep username: | awk '{print $2}')

echo "protocol=$protocol"
echo "host=$host"
echo "username=$username"
echo "password=$password"
